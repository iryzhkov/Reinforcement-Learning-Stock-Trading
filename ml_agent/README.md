# ML Agent

This part of the code is responsible for ml agent.

The agent should be able to:
 - Initialize itself using given configuration
 - Given the world state, return a set of actions to take
 - Given the reward, rework its nn.
